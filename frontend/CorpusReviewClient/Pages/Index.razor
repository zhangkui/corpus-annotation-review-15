@page "/"

<h1>多语言语料标注冲突审查系统</h1>

<div class="card">
    <p>本系统管理多语言语料片段、标注任务、标签层级、标注版本与仲裁结论，核心规则：</p>
    <ul>
        <li><b>重复标注合并展示</b> — 相同片段、同一标签范围的重复标注按组聚合，显示次数与来源标注者。</li>
        <li><b>标签层级不能成环</b> — 创建/改挂标签时沿父链做环检测，成环即拒绝。</li>
        <li><b>仲裁只能引用已有标注版本</b> — 仲裁结论引用已提交版本并生成全新结论，不改原始标注。</li>
        <li><b>分配避免连续同批次</b> — 同一用户不能连续获得同一批次的任务。</li>
        <li><b>导入校验</b> — 校验字符偏移（Unicode 码点）与语言代码（BCP-47）。</li>
    </ul>
</div>

<div class="card">
    <h2>快速入口</h2>
    <p>
        <a href="segments">语料片段</a> ·
        <a href="tags">标签层级</a> ·
        <a href="tasks">任务与分配</a> ·
        <a href="submit-version">提交标注</a> ·
        <a href="arbitrations">仲裁审查</a> ·
        <a href="import">数据导入</a> ·
        <a href="events">事件流</a>
    </p>
    <p class="muted">演示数据：片段 1（中文新闻）已有 alice 与 bob 两个版本的重复标注，可在「语料片段 → 详情」查看合并效果。</p>
</div>
